<strong><%= comment.user().escape('username') %>:</strong><br>
<%= comment.escape('body') %>
<% if (currentUserId == leaderId || currentUserId == comment.user().id) { %>
  <input type="button" value="DELETE" class="delete-comment btn btn-xs">
<% } %>
